#pragma once

#include <cstddef>

/*

rot cutting problem

钢条切割问题（rod cutting problem）：

给定一根长度为 n 的钢条，以及一张价格表（长度和价格的关系如下）：

| 长度 | 价格 |
| --- | --- |
| 1 | 1 |
| 2 | 5 |
| 3 | 8 |
| 4 | 9 |
| 5 | 10 |
| 6 | 17 |
| 7 | 17 |
| 8 | 20 |
| 9 | 24 |
| 10 | 30 |

求切割方案，使得销售收益最大。切割本身没有成本，若切成 k 段
i1 + i2 + ... + ik = n，则总收益为 p_i1 + p_i2 + ... + p_ik。

问题分析：

长度为 n 的钢条，最多可以切割为 n 个钢条（每个钢条长度为 1）。
即，最多有 n-1 个切割点，切割的总可能性为 2^(n-1)。

最优子结构：

对于任意一种切割方案，从左往右看，最左侧一定有一截（不切割对应
最左侧长度为 n），右侧有 0 到 n-1 截。把右侧的钢条合并为一个整体，
只需要求出所有左侧钢条和右侧钢条的价值的最大值即可：左侧钢条是
一个整体，右侧钢条又是一个钢条切割问题，即形成子问题。

递推式：

r(n) = max( p_i + r(n-i) ),   1 <= i <= n,   r(0) = 0

其中 r(n) 表示长度为 n 的钢条的最大收益，p_i 为长度为 i 的整段
钢条的价格。i = n 对应"整段不切"：p_n + r(0)。

若直接按递推式递归求解，子问题被大量重复计算（指数级）；采用
自底向上填表，每个 r(j) 只计算一次，总时间复杂度 O(n^2)，
空间复杂度 O(n)。

*/

class RotCuttingProblem {
public:
    static void registerTest();
};
